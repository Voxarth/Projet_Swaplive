{% extends 'layout.twig' %}

{% block body %}
    <h1>Discussions</h1>
    <ul id="conversations">
        {% for tchat in conversations %}
            <li>
                <a href="/tchat/{{ tchat._id }}">
                    <p><span class="avatar"><img src="{{ tchat.avatar }}"/></span></p>
                    <p><span class="name">{{ tchat.name }}</span></p>
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
